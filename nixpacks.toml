[phases.setup]
nixPkgs = ["ffmpeg", "gcc", "python311", "python311Packages.pip", "libsndfile"]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "gunicorn -c gunicorn_config.py app:app"
